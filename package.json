{
  "name": "medimemory-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build"
  },
  "dependencies": {
    "next": "16.2.9"
  }
}
